# DB locale fichier par défaut (persistée à la racine du repo)
DATABASE_URL=sqlite:///./garageos.db

# Pour basculer sur PostgreSQL, commentez ci-dessus et décommentez :
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/exercice_fastapi

ALLOWED_ORIGINS=["http://localhost:3000"]
APP_NAME=GarageOS
